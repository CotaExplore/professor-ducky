import React from 'react';
import ReactDOM from 'react-dom/client';
import DuckRoom from './pages/DuckRoom';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DuckRoom />
  </React.StrictMode>
);
